tagset    = { 
  environment = "Lab"
  customer    = "Partners"
  sdm-owner   = "foobar@strongdm.com"
	cloud       = "Azure"
}

name      = "partnertraining"

# Azure region (optional - defaults to ukwest)  
# region = "ukwest"

# Network configuration (optional - uses defaults if not specified)
# Note: These are passed to the network module when creating new VNet
# vnet_address_space = ["10.0.0.0/16"]
# gateway_subnet_prefix = "10.0.1.0/24"
# relay_subnet_prefix = "10.0.2.0/24" 
# allowed_ssh_cidr_blocks = ["0.0.0.0/0"]  # Restrict SSH access - use your IP range

create_linux_target = false
create_postgresql = false
create_mssql = false
create_cosmosdb = false
create_domain_controller = false
create_windows_target = false
create_aks = false

create_az_ro = true
create_blob_ro = false
create_blob_full = false

# VM Size Configuration (optional - uses defaults if not specified)
# vm_sizes = {
#   gateway           = "Standard_B1s"
#   relay             = "Standard_B1s" 
#   domain_controller = "Standard_DS1_v2"
#   windows_target    = "Standard_DS1_v2"
#   linux_target      = "Standard_B1s"
#   vault             = "Standard_B1s"
# }

# AKS node size (optional - defaults to Standard_D2_v3)
# aks_node_size = "Standard_D2_v3"

# PostgreSQL database SKU (optional - defaults to B_Standard_B2s)
# postgresql_sku = "B_Standard_B2s"

# Service ports configuration (optional - uses standard defaults)
# Note: StrongDM gateway port is fixed at 5000
# service_ports = {
#   ssh        = 22
#   rdp        = 3389
#   sql_server = 1433
#   postgresql = 5432
#   vault      = 8200
# }

# Administrative usernames (optional - uses defaults)
# admin_usernames = {
#   linux_admin   = "azureuser"
#   windows_admin = "azureadmin"
#   postgres_admin = "pgadmin"
# }

# StrongDM node naming (optional - uses defaults)
# strongdm_naming = {
#   gateway_prefix = "sdm-lab-gateway"
#   relay_prefix   = "sdm-lab-relay"
#   domain_name    = "strongdm.local"
# }

#Onboard domain users to be managed by StrongDM
#create_managedsecrets = true

domain_users = [ 
    {
      "SamAccountName" = "jdoe"
      "GivenName"      = "Jane"
      "Surname"        = "Doe"
      "tags"           = {mfa = "true"}
    },
    {
      "SamAccountName" = "nchandler"
      "GivenName"      = "Noah"
      "Surname"        = "Chandler"
      "tags"           = {justify = "true"}
    },
    {
      "SamAccountName" = "eyang"
      "GivenName"      = "Eva"
      "Surname"        = "Yang"
      "tags"           = {owner = "eva.yang@strongdm.com"}
    }
]
